import React from 'react';
import Button from '@mui/material/Button';

const Login = () => {
  return (
    <div>
      <Button variant="contained" color="primary">Login with Metamask</Button>
    </div>
  );
};

export default Login;
